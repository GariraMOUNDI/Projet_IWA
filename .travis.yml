language: java
jdk:
  - openjdk15
sudo: required
before_install:
  - chmod +x gradlew
services:
  - docker
git:
  depth: false
# safelist
branches:
  only:
    - master
    - alert
    - auth
    - location
    - autre
    - front_web
    - keycloak

#script: ./gradlew clean install
notification:
  email:
    - stuparu.an@gmail.com

